# 이미지 탐색기 사용 방법

이 문서는 설치가 끝난 뒤 실제로 이미지 탐색기를 사용하는 방법을 설명합니다. PNG, JPG/JPEG, BMP, GIF, TIFF, WEBP 등 일반 이미지 파일을 지원합니다. 설치는 [README.md](./README.md)를 참고하세요.

## 1. 접속과 로그인

브라우저에서 실행 주소로 접속합니다.

개발 환경:

```text
http://localhost:5173
```

Docker Compose 운영 환경:

```text
http://<SERVER_IP>:8080
```

로그인 화면에서 `.env` 또는 환경 변수에 설정한 계정을 입력합니다.

- 사용자명: `AUTH_USERNAME`
- 비밀번호: `AUTH_PASSWORD`

현재 로컬 개발 기본값은 `admin / admin`입니다. `AUTH_PASSWORD_HASH`를 사용하는 운영 환경에서는 해시를 만들 때 사용한 원래 비밀번호로 로그인합니다.

## 2. 초기 스캔

`AUTO_SCAN_ON_STARTUP=true`이면 백엔드 시작 시 자동으로 이미지 루트를 스캔합니다.

`PERIODIC_SCAN_INTERVAL_SECONDS=300`이면 백엔드가 실행 중인 동안 5분마다 자동 재스캔합니다. 새 이미지 파일을 복사해 둔 뒤 기다리면 폴더 트리와 검색 결과에 반영됩니다.

`SUPPORTED_IMAGE_EXTENSIONS=.png,.jpg,.jpeg,.jpe,.jfif,.bmp,.gif,.tif,.tiff,.webp,.ico,.jp2,.j2k,.tga` 값으로 탐색할 이미지 확장자를 조절할 수 있습니다.

주기 스캔을 끄려면 값을 `0`으로 설정합니다.

수동으로 다시 스캔하려면 상단의 `관리자` 탭으로 이동한 뒤 `수동 재스캔`을 누릅니다. 수동 재스캔은 `AUTH_USERNAME`으로 설정된 관리자 계정만 실행할 수 있습니다.

API로 요청할 수도 있습니다.

```bash
curl -X POST http://localhost:8000/api/admin/rescan
```

인증이 켜져 있는 운영 환경에서는 관리자 로그인 세션이 필요합니다.

일반 사용자는 탐색 화면에서 `현재 폴더 스캔`을 눌러 선택한 폴더 하위만 재스캔할 수 있습니다. 이 기능은 전체 루트를 다시 훑지 않으므로 일부 폴더만 갱신해야 할 때 CPU 부하를 줄이는 데 유용합니다.

스캔 중복 방지 규칙:

- 관리자 전체 스캔, 주기 스캔, 사용자 폴더 스캔은 동시에 실행되지 않습니다.
- 다른 사용자가 이미 스캔 중이면 `스캔 진행 중` 표시가 뜨고 폴더 스캔 버튼이 비활성화됩니다.
- 스캔이 끝나면 폴더 트리와 이미지 목록이 자동으로 새로고침됩니다.

여러 사용자가 동시에 접속해도 읽기 요청과 스캔 작업이 최대한 충돌하지 않도록 SQLite는 기본적으로 `WAL` 모드와 `30초 busy_timeout`을 사용합니다. 접속자가 많아지고 검색/스캔이 잦아지면 PostgreSQL 전환을 권장합니다.

## 3. 폴더 탐색

상단 `탐색` 탭에서 왼쪽 폴더 트리를 사용합니다.

- `루트`는 `PNG_ROOT_DIR`을 의미합니다.
- 화면에는 절대 경로가 아니라 상대 경로만 표시됩니다.
- 폴더를 클릭하면 해당 폴더 이하 이미지가 검색 결과에 표시됩니다.
- 심볼릭 링크 탐색은 기본적으로 비활성화되어 있습니다.

## 4. 검색과 필터

검색창은 아래 항목을 함께 검색합니다.

- 파일명
- 상대 경로
- 메타데이터 키
- 메타데이터 값

사용 가능한 필터는 다음과 같습니다.

- 메타데이터 키
- 메타데이터 값
- 너비 최소/최대
- 높이 최소/최대
- 파일 크기 최소/최대
- 수정 시작/종료
- 알파 채널 여부
- 상태
- 정렬 기준과 정렬 순서

상태값은 화면에서 한국어로 표시됩니다.

- `정상`
- `손상됨`
- `읽기 불가`
- `지원 안 함`
- `누락됨`

## 5. 이미지 상세 보기

검색 결과의 이미지 카드를 클릭하면 오른쪽 상세 패널이 열립니다.

상세 패널에서 확인할 수 있는 정보는 다음과 같습니다.

- 큰 미리보기
- 파일명
- 상대 경로
- 이미지 크기
- 파일 용량
- 수정 시각
- 포맷과 모드
- 비트 깊이와 컬러 타입
- 전체 메타데이터 테이블

미리보기 도구:

- `축소`
- `확대`
- `화면 맞춤`
- `원본 열기`

`원본 열기`는 백엔드의 안전한 파일 스트리밍 엔드포인트를 통해 열립니다.

## 6. 관리자 화면

상단 `관리자` 탭에서 운영 상태를 확인합니다.

관리자 화면에서 볼 수 있는 정보:

- 스캔 진행 상태
- 활성 이미지 수
- 누락 레코드 수
- 전체 레코드 수
- 마지막 스캔 시작/종료 시각
- 마지막 스캔 결과
- 인증 사용 여부
- 절대 경로 표시 여부
- 상태별 이미지 수
- 주요 디렉터리
- 자주 발견된 메타데이터 키
- 관리자 지정 메타데이터 키
- 이미지 루트 경로

관리자 화면에서 `수동 재스캔`을 누르면 백그라운드 스캔이 시작됩니다. 이 작업은 `AUTH_USERNAME`으로 설정된 관리자 계정만 실행할 수 있으며, 스캔 중에도 탐색 화면은 계속 사용할 수 있습니다.

관리자 화면의 `이미지 루트 경로`에서 현재 서버가 탐색하는 루트 디렉터리를 확인하고 변경할 수 있습니다. 변경한 경로는 SQLite DB의 런타임 설정으로 저장되어 백엔드 재시작 후에도 유지됩니다. 경로를 변경하면 기존 탐색 결과는 안전하게 누락 처리되고, 새 경로에 대해 백그라운드 재스캔이 요청됩니다.

루트 경로 변경 시 주의사항:

- 새 경로는 백엔드 서버에서 실제로 존재하는 디렉터리여야 합니다.
- Windows에서는 예를 들어 `C:\data\company-png`, Ubuntu에서는 `/data/company-png`처럼 서버 기준 경로를 입력합니다.
- 일반 사용자 화면에는 절대 경로가 표시되지 않으며, 이 값은 관리자 화면에서만 확인할 수 있습니다.
- 스캔이 이미 진행 중이면 루트 경로를 바꿀 수 없습니다. 스캔 종료 후 다시 시도하세요.

관리자 지정 메타데이터에는 이미지 목록과 상세 화면에 항상 표시할 키를 등록합니다. 예를 들어 `View`를 등록하면 해당 이미지에 `View=3VV` 메타데이터가 있을 때 `View: 3VV`로 표시되고, 없으면 `View: null`로 표시됩니다. 등록과 삭제는 관리자 계정만 가능합니다.

## 7. 안전 규칙

이 애플리케이션은 내부 시스템 기준으로 보수적으로 동작합니다.

- 원본 이미지 파일은 수정하지 않습니다.
- 썸네일은 `THUMBNAIL_CACHE_DIR`에만 저장합니다.
- 프런트엔드는 절대 경로를 직접 보내지 않습니다.
- 파일 접근은 `image_id` 또는 검증된 상대 경로만 사용합니다.
- `../`, 절대 경로, encoded traversal, symlink escape는 차단됩니다.
- `PUBLIC_SHOW_ABSOLUTE_PATH=false`가 기본값입니다.

## 8. 데이터베이스 백업

Docker Compose 환경에서 SQLite DB를 백업합니다.

```bash
docker compose exec backend sh -lc 'cp /var/lib/png-browser/app.db /var/lib/png-browser/app-$(date +%F-%H%M%S).db'
```

호스트로 복사합니다.

```bash
docker compose cp backend:/var/lib/png-browser/app.db ./app.db.backup
```

## 9. PostgreSQL 전환 메모

이미지가 수십만 건 이상으로 늘어나면 SQLite 대신 PostgreSQL 전환을 권장합니다.

- `DATABASE_URL`을 PostgreSQL DSN으로 변경합니다.
- SQLAlchemy 모델은 대부분 그대로 재사용할 수 있습니다.
- FTS는 PostgreSQL `GIN + tsvector` 또는 전용 검색 인덱스로 교체합니다.
- 썸네일 캐시 구조는 그대로 유지할 수 있습니다.

## 10. 자주 겪는 문제

### 검색 결과가 없습니다

초기 스캔이 끝났는지 관리자 화면에서 확인하세요. 필요하면 `수동 재스캔`을 실행합니다.

### 썸네일이 보이지 않습니다

`THUMBNAIL_CACHE_DIR` 쓰기 권한과 원본 이미지 읽기 권한을 확인하세요.

### 재스캔 요청이 실패합니다

이미 스캔이 진행 중이거나 너무 짧은 간격으로 재요청했을 수 있습니다. 잠시 뒤 다시 시도하세요.

### 절대 경로가 보이지 않습니다

보안 기본값입니다. 꼭 필요할 때만 `PUBLIC_SHOW_ABSOLUTE_PATH=true`로 설정하세요.
