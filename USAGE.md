# 의료 이미지 관리 웹 사용법

이 문서는 앱을 이미 실행한 뒤, 화면에서 무엇을 어떻게 쓰면 되는지 설명합니다. 설치와 실행 방법은 [README.md](./README.md)를 먼저 보세요.

## 접속하기

Docker 로컬 스택으로 실행했다면 다음 주소로 접속합니다.

```text
http://localhost:8080
```

개발 서버로 실행했다면 다음 주소로 접속합니다.

```text
http://localhost:5173
```

기본 관리자 계정은 다음과 같습니다.

```text
사용자명: admin
비밀번호: admin
```

운영 환경에서는 반드시 관리자 비밀번호를 변경하세요.

## 로그인, 회원가입, 비밀번호 변경

### 로그인

로그인 화면에서 사용자명과 비밀번호를 입력합니다.

처음에는 `.env`의 `AUTH_USERNAME`, `AUTH_PASSWORD`로 설정된 관리자 계정으로 로그인합니다.

### 회원가입

로그인 화면의 `회원가입` 탭에서 새 계정을 만들 수 있습니다.

필요한 값:

- 사용자명
- 비밀번호
- 가입 코드

가입 코드는 관리자 페이지에서 바꿀 수 있습니다. 코드가 맞지 않으면 가입되지 않습니다.

### 비밀번호 변경

로그인 후 화면 상단의 `비밀번호 변경` 버튼을 누릅니다.

입력할 값:

- 현재 비밀번호
- 새 비밀번호
- 새 비밀번호 확인

관리자와 일반 사용자 모두 자기 비밀번호를 직접 바꿀 수 있습니다.

## 화면 구성

상단에는 현재 앱 이름, 화면 전환 버튼, 현재 폴더, 비밀번호 변경, 로그아웃 버튼이 있습니다.

주요 화면:

- `탐색`: 이미지 검색과 상세 확인을 하는 기본 화면입니다.
- `관리자`: 관리자만 볼 수 있는 운영 설정 화면입니다.

탐색 화면은 크게 세 영역입니다.

- 왼쪽: 폴더 트리
- 가운데: 검색 조건, 결과 목록, 내보내기 도구
- 오른쪽: 선택한 이미지 상세 정보

## 이미지 스캔

앱은 서버의 이미지 루트 폴더를 스캔해서 DB에 이미지 정보를 저장합니다.

스캔되는 정보:

- 파일명
- 상대 경로
- 파일 크기
- 수정 시각
- 이미지 너비와 높이
- 이미지 포맷
- 알파 채널 여부
- PNG 텍스트 메타데이터
- XMP, EXIF 등 추출 가능한 메타데이터

자동 스캔:

- 백엔드 시작 시 자동 스캔할 수 있습니다.
- 기본 설정에서는 일정 간격으로 재스캔합니다.

수동 스캔:

- 관리자는 관리자 페이지에서 전체 재스캔을 실행할 수 있습니다.
- 일반 사용자는 탐색 화면에서 현재 폴더만 재스캔할 수 있습니다.

삭제된 원본 파일:

- 사용자가 실제 이미지 파일을 지워도 DB는 즉시 바뀌지 않습니다.
- 다음 재스캔 때 해당 이미지 레코드가 DB에서 삭제됩니다.
- 목록에도 더 이상 표시되지 않습니다.

## 폴더 탐색

왼쪽 폴더 트리에서 폴더를 선택하면 해당 폴더 아래 이미지가 검색 결과에 표시됩니다.

알아두면 좋은 점:

- `루트`는 이미지 원본 폴더의 시작점입니다.
- 화면에는 서버 절대 경로가 아니라 상대 경로가 표시됩니다.
- 폴더를 펼치면 하위 폴더를 볼 수 있습니다.
- 폴더별 이미지 개수를 보고 대략적인 규모를 파악할 수 있습니다.

폴더를 바꿨는데 새 파일이 보이지 않는다면 `현재 폴더 재스캔`을 실행하세요.

## 검색과 필터

검색창은 다음 정보를 대상으로 찾습니다.

- 파일명
- 상대 경로
- 메타데이터 키
- 메타데이터 값

사용 가능한 필터:

- 너비 최소/최대
- 높이 최소/최대
- 파일 크기 최소/최대
- 수정 시각 시작/종료
- 알파 채널 여부
- 상태
- 메타데이터 조건
- 정렬 기준
- 정렬 방향

메타데이터 조건은 여러 개 추가할 수 있습니다. 여러 조건을 넣으면 모든 조건을 만족하는 이미지만 표시됩니다.

예시:

```text
key: View
value: 3VV
```

이렇게 입력하면 `View` 메타데이터 값에 `3VV`가 들어간 이미지를 찾습니다.

value를 비워두면 해당 key가 존재하는 이미지를 찾습니다.

## 결과 보기 방식

검색 결과는 세 가지 방식으로 볼 수 있습니다.

- `카드`: 썸네일, 파일명, 경로, 크기, 메타데이터 요약을 함께 봅니다.
- `이미지만`: 썸네일 중심으로 많은 이미지를 빠르게 훑습니다.
- `상세`: 파일 목록처럼 행 단위로 봅니다.

이미지를 클릭하면 오른쪽 상세 패널이 열립니다.

여러 이미지를 선택하려면 카드의 체크박스를 사용합니다. `Shift`를 누른 상태로 다른 이미지를 클릭하면 범위 선택도 할 수 있습니다.

## 이미지 상세 보기

이미지를 선택하면 오른쪽 상세 패널에서 자세한 정보를 볼 수 있습니다.

확인 가능한 정보:

- 미리보기
- 파일명
- 상대 경로
- 파일 크기
- 수정 시각
- 이미지 너비와 높이
- 포맷과 모드
- 비트 깊이
- 색상 정보
- DPI
- 메타데이터 전체 목록
- 관리자가 고정 표시한 메타데이터

상세 패널의 기능:

- 확대
- 축소
- 화면 맞춤
- 원본 열기
- 이미지 정보 새로고침

`이미지 정보 새로고침`은 선택한 이미지 1개만 다시 읽습니다. 파일 크기나 수정 시각이 그대로여도 메타데이터를 강제로 다시 추출합니다.

## 이미지 내보내기

탐색 화면의 `필터 결과 일괄 저장` 영역에서 이미지를 내보낼 수 있습니다.

내보내기 대상은 다음 순서로 정해집니다.

- 체크박스로 선택한 이미지가 있으면 선택한 이미지만 저장합니다.
- 선택한 이미지가 없으면 현재 필터 결과를 저장합니다.

폴더 구조 옵션:

- `한 폴더에 모두 저장`: 모든 파일을 한 폴더에 저장합니다. 이름이 겹치면 번호를 붙입니다.
- `원래 폴더 구조 유지`: 이미지 루트 아래의 상대 경로 구조를 그대로 유지합니다.

### PC 폴더로 저장

`PC 폴더 선택 저장`을 누르면 브라우저의 폴더 선택 창이 열립니다.

권장 브라우저:

- Chrome
- Edge

브라우저가 폴더 선택 API를 지원하지 않으면 이 기능을 사용할 수 없습니다.

### 서버 로컬 폴더로 저장

저장 백엔드에서 `서버 로컬 폴더`를 선택하고, 저장할 폴더명을 입력합니다.

예시:

```text
review-set-2026-05
```

실제 저장 위치는 서버의 `EXPORT_ROOT_DIR` 아래입니다. 관리자 페이지에서 서버 저장 경로를 바꿀 수 있습니다.

### 로컬 MinIO/lakeFS로 저장

저장 백엔드에서 `로컬 MinIO/lakeFS`를 선택합니다.

기본 Docker 스택에서는 MinIO로 저장됩니다.

- MinIO Console: `http://localhost:9001`
- 버킷: `medical-images`
- prefix: `exports`

앱에서 `review-set`이라는 폴더명으로 저장하면 MinIO에는 대략 다음 구조로 올라갑니다.

```text
medical-images/exports/review-set/...
```

lakeFS S3 Gateway로 저장하려면 `.env`를 lakeFS endpoint와 repository 기준으로 바꿔야 합니다.

예시:

```env
OBJECT_STORAGE_ENDPOINT_URL=http://lakefs:8000
OBJECT_STORAGE_BUCKET=medical-images-repo
OBJECT_STORAGE_PREFIX=main/exports
```

그 후 컨테이너를 다시 시작하세요.

```bash
docker compose up -d
```

## 관리자 페이지

관리자 계정으로 로그인하면 상단에 `관리자` 탭이 보입니다.

관리자 페이지에서 할 수 있는 일:

- 전체 재스캔 실행
- 스캔 상태 확인
- 전체 이미지 수 확인
- 기존 누락 레코드 수 확인
- 이미지 루트 경로 확인 및 변경
- 서버 저장 경로 확인 및 변경
- 가입 코드 확인 및 변경
- 고정 표시할 메타데이터 키 추가/삭제
- 주요 폴더와 메타데이터 통계 확인

### 가입 코드 변경

`가입 코드` 섹션에서 새 코드를 입력하고 저장합니다. 이후 회원가입하는 사용자는 새 코드를 입력해야 합니다.

### 이미지 루트 경로 변경

이미지 루트는 백엔드가 스캔할 원본 이미지 폴더입니다.

주의:

- 서버에서 실제로 존재하는 경로여야 합니다.
- Docker에서는 컨테이너 내부 경로를 입력해야 합니다.
- 기본 Docker 구성에서는 `/data/company-png`를 권장합니다.
- 루트를 바꾸면 기존 이미지 DB 레코드는 삭제되고 새 루트가 재스캔됩니다.

### 서버 저장 경로 변경

서버 저장 경로는 `서버 로컬 폴더` 내보내기에서 사용하는 루트입니다.

주의:

- 이미지 루트 내부 경로는 사용할 수 없습니다.
- 서버가 쓰기 권한을 가진 폴더여야 합니다.

### 고정 메타데이터 키

자주 보는 메타데이터 키를 등록하면 이미지 카드와 상세 화면에 항상 표시됩니다.

예시:

```text
View
PatientID
StudyDate
```

값이 없는 이미지는 `null`로 표시됩니다.

## 안전 규칙

앱은 원본 이미지 파일을 직접 수정하지 않습니다.

보안과 안전을 위해 다음 규칙을 적용합니다.

- 원본 파일은 읽기만 합니다.
- 썸네일은 별도 캐시 폴더에 저장합니다.
- 내보내기는 지정된 저장 루트 아래로만 허용합니다.
- `../` 같은 상위 폴더 이동은 차단합니다.
- Windows 드라이브 경로를 내보내기 폴더명으로 쓰는 것을 차단합니다.
- 기본적으로 서버 절대 경로를 일반 화면에 노출하지 않습니다.
- 로컬 MinIO/lakeFS는 로컬 또는 사설망 endpoint만 허용합니다.

## DB와 백업

기본 Docker 구성은 SQLite를 사용합니다.

백업 예시:

```bash
docker compose exec backend sh -lc 'cp /var/lib/png-browser/app.db /var/lib/png-browser/app-$(date +%F-%H%M%S).db'
```

호스트로 복사:

```bash
docker compose cp backend:/var/lib/png-browser/app.db ./app.db.backup
```

이미지 수가 매우 많거나 동시 접속이 많아지면 PostgreSQL 전환을 고려하세요.

## 자주 묻는 질문

### 파일을 넣었는데 목록에 안 보입니다

재스캔이 아직 끝나지 않았을 수 있습니다. 관리자 페이지에서 스캔 상태를 확인하고 수동 재스캔을 실행하세요.

### 실제 파일을 삭제하면 DB에서도 없어지나요?

즉시 없어지지는 않습니다. 다음 재스캔 때 DB 레코드가 삭제되고 목록에서도 사라집니다.

### 썸네일이 안 보입니다

백엔드가 원본 파일을 읽을 수 있는지, `THUMBNAIL_CACHE_DIR`에 쓸 수 있는지 확인하세요.

### MinIO에 저장했는데 파일이 안 보입니다

MinIO Console에서 버킷과 prefix를 확인하세요.

기본 위치:

```text
medical-images/exports/<저장 폴더명>
```

### lakeFS에 바로 저장하고 싶습니다

lakeFS UI에서 repository를 만든 뒤 `.env`를 lakeFS S3 Gateway 기준으로 바꾸세요.

```env
OBJECT_STORAGE_ENDPOINT_URL=http://lakefs:8000
OBJECT_STORAGE_BUCKET=<repository>
OBJECT_STORAGE_PREFIX=main/exports
```

### 관리자 탭이 안 보입니다

현재 로그인한 계정이 관리자 계정이 아닙니다. `.env`의 `AUTH_USERNAME`으로 설정된 계정 또는 관리자 권한 계정으로 로그인하세요.

### 비밀번호를 잊었습니다

관리자 계정의 초기 비밀번호는 `.env`의 `AUTH_PASSWORD` 또는 `AUTH_PASSWORD_HASH` 설정을 확인해야 합니다. 일반 사용자의 비밀번호 초기화 기능은 아직 별도로 제공하지 않습니다.
